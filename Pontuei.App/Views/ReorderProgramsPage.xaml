<?xml version="1.0" encoding="utf-8" ?>
<views:BasePage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Pontuei.App.Views.ReorderProgramsPage"
             xmlns:views="clr-namespace:Pontuei.App.Views"
             xmlns:local="clr-namespace:Pontuei.App.Views"
             Shell.NavBarIsVisible="False"
             NavigationPage.HasNavigationBar="False"
             BackgroundColor="#EDE8E8">

    <Grid>
        <ActivityIndicator IsRunning="{Binding IsLoading}"
                           IsVisible="{Binding IsLoading}"
                           Color="#3A6B4A"
                           VerticalOptions="Center"
                           HorizontalOptions="Center"
                           ZIndex="10" />

<ScrollView VerticalScrollBarVisibility="Never"
                    Padding="0,0,0,110"
                    IsVisible="{Binding IsNotLoading}">
            <VerticalStackLayout Spacing="0" Padding="24,56,24,24">

                <VerticalStackLayout BackgroundColor="Transparent" Spacing="0">
                    <VerticalStackLayout.GestureRecognizers>
                        <DropGestureRecognizer AllowDrop="True" Drop="OnTopAreaDrop" />
                    </VerticalStackLayout.GestureRecognizers>

                    <ContentView x:Name="CardPos1" IsVisible="{Binding HasPosition1}" Margin="0,0,0,16">
                        <Border BindingContext="{Binding Position1}" HeightRequest="180" StrokeThickness="0" Padding="0">
                            <Border.StrokeShape>
                                <RoundRectangle CornerRadius="24" />
                            </Border.StrokeShape>
                            <Border.Shadow>
                                <Shadow Brush="Black" Offset="0,12" Radius="15" Opacity="0.3" />
                            </Border.Shadow>

                            <Border.GestureRecognizers>
                                <DragGestureRecognizer CanDrag="True" DragStarting="OnDragStarting" />
                                <DropGestureRecognizer AllowDrop="True" Drop="OnDrop" />
                            </Border.GestureRecognizers>

                            <AbsoluteLayout>
                                <Border AbsoluteLayout.LayoutFlags="All" AbsoluteLayout.LayoutBounds="0,0,1,1" StrokeThickness="0">
                                    <Border.StrokeShape>
                                        <RoundRectangle CornerRadius="24" />
                                    </Border.StrokeShape>
                                    <Border.Background>
                                        <RadialGradientBrush Center="0.5,0.5" Radius="0.85">
                                            <GradientStop Color="{Binding SecondaryColor}" Offset="0.0" />
                                            <GradientStop Color="{Binding PrimaryColor}" Offset="1.0" />
                                        </RadialGradientBrush>
                                    </Border.Background>
                                </Border>

                                <Grid AbsoluteLayout.LayoutFlags="All" AbsoluteLayout.LayoutBounds="0,0,1,1">
                                    <Image Source="{Binding LogoUrl}" HeightRequest="40" Aspect="AspectFit" HorizontalOptions="Center" VerticalOptions="Center" IsVisible="{Binding HasLogo}" />
                                    <Label Text="{Binding Name}" TextColor="White" FontFamily="PoppinsSemibold" FontAttributes="Bold" FontSize="20" HorizontalOptions="Center" VerticalOptions="Center" IsVisible="{Binding ShowProgramName}" />

                                    <Border BackgroundColor="White" WidthRequest="24" HeightRequest="24" HorizontalOptions="End" VerticalOptions="Start" Margin="16" StrokeThickness="0">
                                        <Border.StrokeShape>
                                            <RoundRectangle CornerRadius="12" />
                                        </Border.StrokeShape>
                                        <Image Source="star.svg" WidthRequest="12" HeightRequest="12" HorizontalOptions="Center" VerticalOptions="Center" />
                                    </Border>
                                </Grid>
                            </AbsoluteLayout>
                        </Border>
                    </ContentView>

                    <Grid ColumnDefinitions="*,*" ColumnSpacing="16" Margin="0,0,0,24">

                        <ContentView x:Name="CardPos2" Grid.Column="0" IsVisible="{Binding HasPosition2}">
                            <Border BindingContext="{Binding Position2}" HeightRequest="100" StrokeThickness="0" Padding="0">
                                <Border.StrokeShape>
                                    <RoundRectangle CornerRadius="16" />
                                </Border.StrokeShape>
                                <Border.Shadow>
                                    <Shadow Brush="Black" Offset="0,6" Radius="10" Opacity="0.2" />
                                </Border.Shadow>
                                <Border.GestureRecognizers>
                                    <DragGestureRecognizer CanDrag="True" DragStarting="OnDragStarting" />
                                    <DropGestureRecognizer AllowDrop="True" Drop="OnDrop" />
                                </Border.GestureRecognizers>

                                <AbsoluteLayout>
                                    <Border AbsoluteLayout.LayoutFlags="All" AbsoluteLayout.LayoutBounds="0,0,1,1" StrokeThickness="0">
                                        <Border.StrokeShape>
                                            <RoundRectangle CornerRadius="16" />
                                        </Border.StrokeShape>
                                        <Border.Background>
                                            <RadialGradientBrush Center="0.5,0.5" Radius="0.85">
                                                <GradientStop Color="{Binding SecondaryColor}" Offset="0.0" />
                                                <GradientStop Color="{Binding PrimaryColor}" Offset="1.0" />
                                            </RadialGradientBrush>
                                        </Border.Background>
                                    </Border>

                                    <Grid AbsoluteLayout.LayoutFlags="All" AbsoluteLayout.LayoutBounds="0,0,1,1">
                                        <Image Source="{Binding LogoUrl}" HeightRequest="24" Aspect="AspectFit" HorizontalOptions="Center" VerticalOptions="Center" IsVisible="{Binding HasLogo}" />
                                        <Label Text="{Binding Name}" TextColor="White" FontFamily="PoppinsSemibold" FontAttributes="Bold" FontSize="14" HorizontalOptions="Center" VerticalOptions="Center" IsVisible="{Binding ShowProgramName}" />
                                        <Border BackgroundColor="White" WidthRequest="16" HeightRequest="16" HorizontalOptions="End" VerticalOptions="Start" Margin="8" StrokeThickness="0">
                                            <Border.StrokeShape>
                                                <RoundRectangle CornerRadius="8" />
                                            </Border.StrokeShape>
                                            <Label Text="2" FontSize="9" FontFamily="PoppinsSemibold" TextColor="#1A1A1A" HorizontalOptions="Center" VerticalOptions="Center" />
                                        </Border>
                                    </Grid>
                                </AbsoluteLayout>
                            </Border>
                        </ContentView>

                        <ContentView x:Name="CardPos3" Grid.Column="1" IsVisible="{Binding HasPosition3}">
                            <Border BindingContext="{Binding Position3}" HeightRequest="100" StrokeThickness="0" Padding="0">
                                <Border.StrokeShape>
                                    <RoundRectangle CornerRadius="16" />
                                </Border.StrokeShape>
                                <Border.Shadow>
                                    <Shadow Brush="Black" Offset="0,6" Radius="10" Opacity="0.2" />
                                </Border.Shadow>
                                <Border.GestureRecognizers>
                                    <DragGestureRecognizer CanDrag="True" DragStarting="OnDragStarting" />
                                    <DropGestureRecognizer AllowDrop="True" Drop="OnDrop" />
                                </Border.GestureRecognizers>

                                <AbsoluteLayout>
                                    <Border AbsoluteLayout.LayoutFlags="All" AbsoluteLayout.LayoutBounds="0,0,1,1" StrokeThickness="0">
                                        <Border.StrokeShape>
                                            <RoundRectangle CornerRadius="16" />
                                        </Border.StrokeShape>
                                        <Border.Background>
                                            <RadialGradientBrush Center="0.5,0.5" Radius="0.85">
                                                <GradientStop Color="{Binding SecondaryColor}" Offset="0.0" />
                                                <GradientStop Color="{Binding PrimaryColor}" Offset="1.0" />
                                            </RadialGradientBrush>
                                        </Border.Background>
                                    </Border>

                                    <Grid AbsoluteLayout.LayoutFlags="All" AbsoluteLayout.LayoutBounds="0,0,1,1">
                                        <Image Source="{Binding LogoUrl}" HeightRequest="28" Aspect="AspectFit" HorizontalOptions="Center" VerticalOptions="Center" IsVisible="{Binding HasLogo}" />
                                        <Label Text="{Binding Name}" TextColor="White" FontFamily="PoppinsSemibold" FontAttributes="Bold" FontSize="14" HorizontalOptions="Center" VerticalOptions="Center" IsVisible="{Binding ShowProgramName}" />
                                        <Border BackgroundColor="White" WidthRequest="16" HeightRequest="16" HorizontalOptions="End" VerticalOptions="Start" Margin="8" StrokeThickness="0">
                                            <Border.StrokeShape>
                                                <RoundRectangle CornerRadius="8" />
                                            </Border.StrokeShape>
                                            <Label Text="3" FontSize="9" FontFamily="PoppinsSemibold" TextColor="#1A1A1A" HorizontalOptions="Center" VerticalOptions="Center" />
                                        </Border>
                                    </Grid>
                                </AbsoluteLayout>
                            </Border>
                        </ContentView>
                    </Grid>
                </VerticalStackLayout>

                <FlexLayout x:Name="OtherCardsContainer" BindableLayout.ItemsSource="{Binding OtherPrograms}" Wrap="Wrap" JustifyContent="SpaceBetween" Margin="0,0,0,16">
                    <FlexLayout.GestureRecognizers>
                        <DropGestureRecognizer AllowDrop="True" Drop="OnFlexLayoutDrop" />
                    </FlexLayout.GestureRecognizers>
                    <BindableLayout.ItemTemplate>
                        <DataTemplate x:DataType="local:ReorderProgramItem">
                            <Border WidthRequest="100" HeightRequest="56" BackgroundColor="White" StrokeThickness="0" Margin="0,0,0,12">
                                <Border.StrokeShape>
                                    <RoundRectangle CornerRadius="16" />
                                </Border.StrokeShape>
                                <Border.Shadow>
                                    <Shadow Brush="Black" Offset="0,4" Radius="6" Opacity="0.04" />
                                </Border.Shadow>
                                <Border.GestureRecognizers>
                                    <DragGestureRecognizer CanDrag="True" DragStarting="OnDragStarting" />
                                    <DropGestureRecognizer AllowDrop="True" Drop="OnDrop" />
                                </Border.GestureRecognizers>
                                <Grid Margin="8">
                                    <Image Source="{Binding LogoUrl}" HeightRequest="24" Aspect="AspectFit" HorizontalOptions="Center" VerticalOptions="Center" IsVisible="{Binding HasLogo}" />
                                    <Label Text="{Binding Name}" TextColor="#1A1A1A" FontFamily="PoppinsSemibold" FontSize="11" HorizontalOptions="Center" VerticalOptions="Center" LineBreakMode="TailTruncation" IsVisible="{Binding ShowProgramName}" />
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </BindableLayout.ItemTemplate>
                </FlexLayout>

                <BoxView HeightRequest="1" Color="#D0C8C8" Margin="0,8,0,24" />

                <Grid ColumnDefinitions="*, *" ColumnSpacing="16" Margin="0,0,0,16">
                    <Button Grid.Column="0" Text="Cancelar" TextColor="White" BackgroundColor="#9A5C5C" CornerRadius="12" HeightRequest="48" FontFamily="PoppinsSemibold" FontSize="15" IsEnabled="{Binding IsNotSaving}" Clicked="OnCancelClicked" />
                    <Button Grid.Column="1" Text="{Binding SaveButtonText}" TextColor="White" BackgroundColor="#578E60" CornerRadius="12" HeightRequest="48" FontFamily="PoppinsSemibold" FontSize="15" IsEnabled="{Binding IsNotSaving}" Clicked="OnSaveClicked" />
                </Grid>

            </VerticalStackLayout>
        </ScrollView>

    </Grid>
</views:BasePage>